# Render backend env vars
# Render sets RENDER_EXTERNAL_URL automatically — no need to set it manually

# Your Vercel frontend URL (used for CORS)
FRONTEND_URL=https://your-app.vercel.app

# Port (Render sets this automatically too)
PORT=4000
